<h1>Company: <%= @company.name %></h1>
<p>Email: <%= @company.email %></p>

<section class="new-connections">
  <h2 class="new-connections__heading">Add a new connection</h2>
  <ul class="new-connections__list">
    <% %w[git_hub slack jira heroku google].each do |provider| %>
      <li class="new-connections__item">
        <%= link_to route_for(provider), title: provider.camelize, class: "new-connections__button", target: :_blank, role: :button do %>
          <%= render "icons/#{provider}", classes: 'new-connections__icon' %>
        <% end %>
      </li>
    <% end %>
  </ul>
</section>

<%= button_to 'Reaudit', [:account, :audit],
              data: { controller: 'buttons--busy',
                      action: 'buttons--busy#busy' } %>

<% @company.ext__resources.each do |resource| %>
  <%= render "icons/#{resource.provider.underscore}", classes: 'connection__icon' %>
  <%= resource.name %>

  <ul>
  <% resource.personas.each do |persona| %>
    <li>
      <%= render persona %>
    </li>
  <% end %>
  </ul>
<% end %>

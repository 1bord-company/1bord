google:
  base_url: oauth2.googleapis.com
  token_path: token
  resources_base_url: admin.googleapis.com
  resources:
    users:
      path: admin/directory/v1/users?customer=my_customer
      actions:
        - index
